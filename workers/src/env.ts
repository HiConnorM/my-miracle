/**
 * The Worker's bindings.
 *
 * Generated from `wrangler.jsonc` by `wrangler types` (see `worker-configuration.d.ts`),
 * so adding a binding to the config is enough — there is no second list to keep in step.
 * Secrets are declared in `src/env.d.ts`.
 */
export type Env = Cloudflare.Env;
